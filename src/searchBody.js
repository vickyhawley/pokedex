import React from 'react';
import { View, Text } from 'react-native';

const searchBody = () => {
        return(
            <View>
                <Text>Data</Text>
            </View>
        )
    };

export default searchBody;